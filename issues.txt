смена языка должна быть через <select> принципиально

линт надо активировать в проекте, исправить все ошибки относительно того что выдаёт, либо настроить так чтобы не выдавал ошибок,
если это принятый код стайл

где .editorconfig? писал в скайпе в общий чат

locales/en.json и locales/ru.json, не надо лишней вложенности

t('choose_a_time')
и все похожие должны быть t('CHOOSE_A_TIME')
потому что это константы, их пишут заглавными буквами

UI улучшился, вижу табы, это хорошая идея
----------------------------------------------

текст коммита надо писать на английском, так принято в программировании, если в проекте не оговорено обратного

https://prnt.sc/1xdsadm
у меня вот так вышло с установкой зависимостей. Проверь свою версию npm и сделай npm install -E
и сделай коммит с новыми файлами, если тоже будет ошибка, то удаляй lock файл и делай npm install -E

https://prnt.sc/1xdu566
ты написала что не можешь избавиться от ворнингов. В ворнинге говорится что ты экспортировала без имени.
Для примера пофиксил в PickersBodyWeekTable.js

ты сделала ширину скролла меньше, соответственно этим элементом стало сложнее пользоваться, пострадал UX в угоду дизайну, не надо так

лейаут для мобилки не адаптивный, надо разобраться с этим

что-то случилось с лейаутом страницы
https://prnt.sc/1xdvqdq
при такой высоте экрана не могу увидеть нижней таблицы.
Мое предложение следующее: скролл у страницы надо оставить всё же один без дополнительных областей для скролла
Кол-во контента в каждой таблице лучше делать через пагинацию с ограничением в 10 записей.
Ещё лучше это не выводить столько контента одновременно
-----------------------

основная проблема проекта связана с внешним видом, UI перегружен, его обязательно стоит дробить на страницы/модалки,
как вариант добавлять возможность делать коллапс блоков, чтобы пользователь не испытывал перегруз информации.
По крайней мере есть смысл сделать basic/advanced view, когда будет кнопка переключающая UI между простым/высоконагруженным внешним видом

Отсутствует адаптив, на мобилках всё плохо (совсем и с этим не получится поспорить),
вместо реакта надо было отдать силы mobile first, это надо исправлять

Лейаут проекта имеет отступы по краям у шапку и у футера, выглядит неуместно

Огромное количество warnings, которые обязательно надо исправлять
Ты отдаешь это код ревью, хотя сам проект уже подсказывает что можно исправить, никогда так не делай

В проекте есть мусор, который остался от стандартной установки реакта, логотипы/иконки удалить или заменить на подходящие

форматирование прыгает, много ненужных пустых строк (почти в 100% случаев), не должно встречаться больше одной пустой строки подряд.
Также текст страдает от неправильного форматирования, ещё раз напомню что мне приходится читать его, а это затруднительно если
не соблюден минимум для написанного кода
