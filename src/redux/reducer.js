const initiaState = {
	events: [],
	event: {},
	loading: false
};

const eventsReducers = (state = initiaState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default eventsReducers;